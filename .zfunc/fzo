LOCATION=$(fd . $1 | fzf --preview 'stat -x {}' --preview-window=up)

# Catches if you push <esc> or <C-c> from inside fzf and aborts
if [[ "$LOCATION" ]]; then;
    open "$LOCATION"
    exit 0
else
    return 1 
fi

